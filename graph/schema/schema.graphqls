# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Query {
  user: User!
  posts(cursor: String): PostsResponse
  post(id: String!): Post!
}

type Paginator {
  next_cursor: String
}

type PostsResponse {
  data: [Post]!
  paginator: Paginator
}
